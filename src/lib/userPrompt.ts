export const buildUserPrompt = (rawInput: string, mode: string) => `
Текст для анализа: "${rawInput}"
Контекст (режим): ${mode === 'we' ? 'совместный' : 'личный'}

Инструкция: Проанализируй текст выше и структурируй его строго по категориям, описанным в системной схеме. 
Если информации недостаточно для какой-то категории, не включай её в массив.
`;